package testing;

import org.junit.Assert;
import org.junit.Test;
import java.util.ArrayList;
import java.io.*;
import util.DataReader;
import util.Movie;
import util.Place;
import util.Time;

public class UtilTests {
/*
    @Test
    protected static void placeTest() {
        Assert.assertEquals("Fitzroy", Place.FITZROY.place);
        Assert.assertEquals("St Kilda", Place.STKILDA);
        Assert.assertEquals("Melbourne CBD", Place.MELBCBD);
        Assert.assertEquals("Lilydale", Place.LILYDALE);
        Assert.assertEquals("Sunshine", Place.SUNSHINE);
        //System.out.println("All place tests passed! :)");
    }

    @Test
    protected static void movieTest() {

        Movie testMovie = new Movie();
        //Time movieTime = new Time(1,1,1,1,1,1);



        testMovie.initializeMovie("Ro-Kyu-Bu SS",
               movieTime , Place.MELBCBD);

       Assert.assertEquals("Ro-kyu-bu", testMovie.getMovieInformation()[0]);



    }

    @Test
    protected static void timeTest() {

        //Time testTime = new Time(1,1,1,1,1,1);

        //Assert.assertEquals(1, testTime.year);
        //Assert.assertEquals(1, testTime.month);
        Assert.assertEquals(1, testTime.day);

        Assert.assertEquals(1, testTime.hour);
        Assert.assertEquals(1, testTime.minute);
        Assert.assertEquals(1, testTime.second);

    }

    @Test
    protected static void dataReaderTest() {

        ArrayList<Movie> testMovieArray;
        File file = new File("./Timetable_data");

        //testMovieArray = DataReader.getDataReader().movieDataReader(file);

    }
*/

}
